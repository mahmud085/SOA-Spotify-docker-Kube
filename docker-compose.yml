version: '3'
services:
  web:
    build: ./web
    restart: always
    depends_on:
      - "images"
      - "charts"
    ports:
      - "8888:3000"
  images:
    build: ./images
    restart: always
  charts:
    build: ./charts
    restart: always